# Web 前端轨道

**完整教材（非 30 天合订）** — 连续章节式 Vue 3 学习控制台教科书：

→ [WEB_VUE_COMPLETE.md](./lessons/WEB_VUE_COMPLETE.md)

**技术栈：** Vue 3 + Vite + `<script setup>`；推荐 Vue Router + Pinia；CSS 变量设计令牌。开发期 Vite `server.proxy` → Spring Boot `http://localhost:8080`（`/api`）；生产学习路径：`vite build` 拷入 `erp-ai-assistant/src/main/resources/static/` 或 `vite preview`。

**约定：** 纯学习；骨架粘贴到 `erp-ai-console/`；**勿**改 `erp-ai-assistant/` 应用代码；**不接公司生产**。

**旧版说明：** 原 `WEB-D1～30` 逐日合订已退役 → [WEB_DAY1-30_COMBINED.md](./lessons/WEB_DAY1-30_COMBINED.md)（重定向 stub）
