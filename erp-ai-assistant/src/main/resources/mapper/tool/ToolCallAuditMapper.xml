<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.ai.tool.mapper.ToolCallAuditMapper">

    <insert id="insertAudit" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tool_call_audit(tool_name, ok, args_keys, error_message, latency_ms)
        VALUES (#{toolName}, #{ok}, #{argsKeys}, #{errorMessage}, #{latencyMs})
    </insert>

</mapper>
