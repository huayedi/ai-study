<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.ai.chat.mapper.ChatSessionMessageMapper">

    <select id="selectBySessionId" resultType="com.erp.ai.chat.entity.ChatSessionMessage">
        SELECT id, session_id AS sessionId, role, content, created_at AS createdAt
        FROM chat_session_message
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
    </select>

    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session_message(session_id, role, content)
        VALUES (#{sessionId}, #{role}, #{content})
    </insert>

    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM chat_session_message WHERE session_id = #{sessionId}
    </select>

    <select id="selectOldestIds" resultType="long">
        SELECT id FROM chat_session_message
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <delete id="deleteMessageById">
        DELETE FROM chat_session_message WHERE id = #{id}
    </delete>

</mapper>
